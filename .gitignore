# Dingo run-time output
Logs/
Tests/Phase4/Evidence/
Dingo-*.zip

# Windows Terminal backups written by Dingo
*.backup-*
*.replace-backup-*

# Your own tool catalog. Tools.example.json is the template that is tracked.
Tools.json

# Working screenshots. Only the redacted copy referenced by the README is
# tracked, so an unredacted shot cannot be published by a stray 'git add -A'.
Assets/Dingo-main-screen.png
